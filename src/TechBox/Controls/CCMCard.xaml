<UserControl x:Class="TechBox.Controls.CCMCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TechBox.Controls"
             xmlns:helpers="clr-namespace:TechBox.Helpers"
			 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" 
             mc:Ignorable="d" 
			 Height="90" MaxHeight="90"
             d:DesignHeight="90" d:DesignWidth="250">

    <UserControl.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibility" />
        <helpers:InvertBoolToVisibilityConverter x:Key="InvertBoolToVisibilityConverter" />
    </UserControl.Resources>
    
    <Grid>
		<Border 
            Margin="0,10,10,0"
            Background="#3c3c3c"
            BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1,1,1,1"
            CornerRadius="8,8,8,8"
            >
			<StackPanel>
				<Grid Margin="10,8">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>
					<StackPanel Grid.Column="0" Orientation="Horizontal">
                        <Label Content="{Binding ComputerName, RelativeSource={RelativeSource AncestorType=UserControl}, FallbackValue=ComputerName}" />
                    </StackPanel>
                    
                    <ui:SymbolIcon Grid.Column="1" Margin="10,-5,0,0" Filled="True"
                        Symbol="{Binding SymbolIcon, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}, FallbackValue=PlugConnected24}"
                        Foreground="{Binding IconColor, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}, FallbackValue=Green}"
                        Visibility="{Binding Ping, Converter={StaticResource InvertBoolToVisibilityConverter}}"
                        />

                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <ui:Button Icon="{Binding ButtonIcon, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}" MaxHeight="25" Command="{Binding CancelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" />
					</StackPanel>
				</Grid>

                <ProgressBar Margin="10,5"
                    IsIndeterminate="{Binding IsIndeterminate, RelativeSource={RelativeSource AncestorType=UserControl}, FallbackValue=true}" 
                    Minimum="0"
                    Maximum="{Binding MaxPercent, RelativeSource={RelativeSource AncestorType=UserControl}, FallbackValue=0}"
                    Value="{Binding PercentComplete, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}, FallbackValue=0}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <ui:TextBlock Grid.Column="0" Margin="10,0" Text="{Binding CurrentAction, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Label Margin="25,0,0,0" Content="{Binding DisplayPercentComplete, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}, FallbackValue=0}" />
                        <Label Margin="2,0,15,0" Content="%" />
                    </StackPanel>
                </Grid>
            </StackPanel>
		</Border>
	</Grid>
</UserControl>
