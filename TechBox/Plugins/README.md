# Plugins

Drop plugin `.dll` files (with their dependencies) directly in a `Plugins` folder next to
`TechBox.exe`. TechBox scans it on startup and loads every assembly it finds there (not in
subfolders).

See the main [README](../../README.md#plugins) for how to write a plugin.
