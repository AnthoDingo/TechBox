<Page
    x:Class="TechBox.Views.Pages.Computers.PowerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:TechBox.Views.Pages.Computers"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:helpers="clr-namespace:TechBox.Helpers"
    Title="PowerPage"
    d:DataContext="{d:DesignInstance local:PowerPage,
                                 IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Page.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:AutoSuggestBox Grid.Row="0"
            HorizontalAlignment="Left"
            MinWidth="200"
            OriginalItemsSource="{Binding ViewModel.Computers, Mode=OneWay}"
            PlaceholderText="Computer Name..."
            SuggestionChosen="AutoSuggestBox_SuggestionChosen"
            QuerySubmitted="AutoSuggestBox_QuerySubmitted"/>

        <ui:Card Grid.Row="1" Margin="0,15,0,0" HorizontalAlignment="Left" MinWidth="300"
            VerticalContentAlignment="Top"
            Visibility="{Binding ViewModel.HasSelection, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel>
                <TextBlock Text="{Binding ViewModel.SelectedCompter.Name, Mode=OneWay}" FontSize="20" FontWeight="Medium" />

                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <ui:SymbolIcon Symbol="Power20" FontSize="18" VerticalAlignment="Center" />
                    <TextBlock Text="{Binding ViewModel.SelectedCompter.UptimeAsHuman, Mode=OneWay}"
                        Margin="8,0,0,0" VerticalAlignment="Center" FontSize="16" />
                </StackPanel>

                <TextBlock Text="{Binding ViewModel.StatusMessage, Mode=OneWay}" Margin="0,8,0,0"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
            </StackPanel>
        </ui:Card>
    </Grid>
</Page>
